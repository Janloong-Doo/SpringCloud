spring:
  application:
    name: adh-consume
  datasource:
    url:  jdbc:mysql://127.0.0.1:3306/sg_datacenter?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC&useSSL=false
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  http:
    converters:
      preferred-json-mapper: jackson

server:
  port: 8081
  servlet:
    context-path: /rzt
mybatis:
  check-config-location: true
  mapper-locations: classpath:mybatis/mapper/*.xml
  config-location: classpath:mybatis/mybatis-config.xml
