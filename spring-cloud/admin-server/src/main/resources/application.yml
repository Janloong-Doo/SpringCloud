spring:
  application:
    name: admin-server
  boot:
    admin:
      contextPath: /admin    # 配置访问路径为：http://localhost:8889/admin
server:
  port: 8889
eureka:
  client:
    serviceUrl:
#      default-zone: http://localhost:8761/eureka/
      defaultZone: http://localhost:8762/eureka/