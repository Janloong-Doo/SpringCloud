spring:
  profiles:
    active: client1
#  rabbitmq:
#    port: 5672
#    host: 127.0.0.1
#    username: guest
#    password: guest

management:
  endpoints:
    web:
      exposure:
        include: "*"
from:
  config-server: ${janloong.eureka.ip}